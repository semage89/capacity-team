{
  "name": "team-capacity-planner",
  "version": "1.0.0",
  "description": "Team Capacity Planner - Planowanie czasu pracy zespołu IT",
  "scripts": {
    "heroku-prebuild": "echo '=== Heroku Prebuild ===' && pwd && ls -la",
    "heroku-postbuild": "echo '=== Heroku Postbuild ===' && if [ -d frontend ]; then cd frontend && npm install && npm run build && echo 'Build completed'; else echo 'Frontend dir not found'; fi"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
