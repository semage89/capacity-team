{
  "name": "capacity-team-planner",
  "version": "1.0.0",
  "description": "Capacity Team Planner - Planowanie pojemności zespołu z Jira i Tempo",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python app.py",
    "heroku-prebuild": "echo '=== Heroku Prebuild: Installing frontend dependencies ===' && cd frontend && npm install",
    "heroku-postbuild": "echo '=== Heroku Postbuild: Building frontend ===' && cd frontend && npm run build && echo '=== Frontend build completed! ==='"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ],
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}

